apiVersion: apps/v1
kind: Deployment
metadata:
  name: location-service
  labels: { app.kubernetes.io/name: location-service }
spec:
  replicas: 1
  selector:
    matchLabels: { app.kubernetes.io/name: location-service }
  template:
    metadata:
      labels: { app.kubernetes.io/name: location-service }
    spec:
      containers:
        - name: location-service
          image: assettracker/location-service:latest
          ports:
            - { name: http, containerPort: 8084 }
          envFrom:
            - configMapRef: { name: asset-tracker-env }
            - secretRef: { name: asset-tracker-db }
          env:
            - { name: SERVER_PORT, value: "8084" }
            - name: PG_HOST
              valueFrom: { configMapKeyRef: { name: asset-tracker-env, key: PG_HOST } }
            - name: PG_PORT
              valueFrom: { configMapKeyRef: { name: asset-tracker-env, key: PG_PORT } }
            - name: PG_OPTS
              valueFrom: { configMapKeyRef: { name: asset-tracker-env, key: PG_OPTS } }
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://$(PG_HOST):$(PG_PORT)/locationdb$(PG_OPTS)"
          resources:
            requests: { cpu: "100m", memory: "384Mi" }
            limits: { cpu: "1", memory: "768Mi" }
          startupProbe:
            httpGet: { path: /actuator/health, port: http }
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: http }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: http }
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: location-service
  labels: { app.kubernetes.io/name: location-service }
spec:
  selector: { app.kubernetes.io/name: location-service }
  ports:
    - { name: http, port: 8084, targetPort: http }
